#include <stdio.h>
/* more headers goes there */
/* betty style doc for function main goes there */
/**
* main - Entry point
* Return: Always 0 (Success)
*/
int main(void)
{
	for (i = 0; i < 100; p++)
	{
		for (p = 0; p < 100; p++)
			if (i < j)
			{
				putchar((i / 10) + 48);
				putchar((i % 10) + 48);
				putchar(' ');
				putchar((p / 10) + 48);
				putchar((p % 10) + 48);
				if (i != 98 || p != 99)
				{
					putchar(',');
					putchar(' ');
				}
			}
	}
}
putchar('\n');
return (0);
}
